from setuptools import setup, find_packages

setup(name='symbolic_representation',
      version='0.0.1',
      install_requires=['gym','keras-rl'],
      author='Ben Striner',
      url='https://github.com/bstriner/symbolic-representation',
      packages=find_packages())
